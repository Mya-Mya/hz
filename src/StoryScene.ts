import { Scene } from "./Scene";
import P5 from "p5"

export class StoryScene implements Scene{
    on_enter(p:P5): void {
        
    }
    draw(p: P5): void {
        
    }
    mousePressed(e: object, p:P5): void {
        
    }
}